{
  "name": "fs-regex-mcp",
  "version": "1.0.0",
  "description": "MCP server providing regex-based file operations for LLM programming assistants",
  "main": "dist/index.js",
  "type": "module",
  "bin": {
    "fs-regex-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "prepare": "npm run build"
  },
  "keywords": [
    "mcp",
    "regex",
    "file-operations",
    "search",
    "replace",
    "llm"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.22.0",
    "fast-glob": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.3.3",
    "vitest": "^4.0.13"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
